package ver0_problem;

// oracle 버전으로 작성된 Repository 클래스. DB SQL 실행을 담당함.(INSERT,UPDATE,DELETE,SELECT 밖에 모르는 바아보)
public class ChinaTire {
    String model = "대륙타이어";

    public String getTireModel(){
        return model;
    }
}
