package test01

fun main() {
    // Array 클래스로 객체생성. Array(size, {""}) 또는 Array(size){초기화값}
    // 코틀린의 변수는 전역, 멤버, 지역이고 이중에 지역은 초기화 자체가 필수 아니지만 전역,멤버는 초기값 필수(자바에서는 null 자동이었던...)
    // 여기서는 Array도 클래스니까 그 내부 배열 칸들을 멤버변수들 이라고 생각하겠지.. 그니까 초기값 명시적으로 넣어라넣어라넣어라
    var emptyArray = Array<String>(3){"abc"}
    emptyArray[0] = "hello"

    // 배열 각 칸의 식별은 인덱스로 하게됨. 즉, 각 변수 이름이 0,1,2 라고 생각하면 그 식별값을 넣고 모든 변수에게 각각 람다 함수를 실행시켜 넣어준다고 이해할 수 있겠네요.
    var arr2 = Array(3){n->n*2}
}